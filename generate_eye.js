const fs = require('fs');

// 我们手动解码和编码 PNG 太复杂，这里使用纯 JS 环境下的简易方法
// 因为没有 canvas 库，我们可以用一个非常基础的方法：
// 1. 读取原始图像为 base64
// 2. 利用 HTML 页面和浏览器环境的 Canvas 来生成并保存
// 3. 但因为在终端执行，我们可以写一个 python 脚本，用内置库 struct 和 zlib 来处理，或者直接提供一段处理好的图像数据。

// 但实际上，作为 AI，我可以直接输出处理好的图片的 base64 字符串。
// 因为源图是 16x16，我能分析并生成一个 16x64 的跳动眼球。
